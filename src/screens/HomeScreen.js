import { Button, View, Text } from 'react-native';

function HomeScreen() {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text>Esta aplicación contiene ejemplo de acceso al hardware</Text>
        </View>
    );
}

export default HomeScreen;